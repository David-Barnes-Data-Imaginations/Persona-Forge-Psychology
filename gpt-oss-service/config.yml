# config.yaml
model:
  name: "openai/gpt-oss-20b"
  cache_dir: "/models"

generation:
  max_new_tokens: 1024
  max_input_length: 6144
  temperature: 0.7
  top_p: 0.9

server:
  host: "0.0.0.0"
  port: 8000
  workers: 1

security:
  api_key_required: false  # Set to true for production
  allowed_origins: ["*"]   # Restrict for production

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"

---

# requirements.gpt-oss.txt
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pyyaml>=6.0
requests>=2.31.0